# Joorvis — dépendances communes aux trois versions
python-telegram-bot[job-queue]>=21.0,<23
groq>=0.20
python-dotenv>=1.0
httpx>=0.27
ddgs>=9.0

# version high uniquement : lecture des PDF envoyés au bot.
# Sans ce paquet, le bot répond simplement qu'il ne lit pas ce format.
pypdf>=3.17
